@page
@model Societate_de_Valori.Pages.Firme.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Firma[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Firma[0].NumarAngajati)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Firma[0].NumeActiune)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Firma[0].Tara)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Firma) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumarAngajati)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeActiune)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tara)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
